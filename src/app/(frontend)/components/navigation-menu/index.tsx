import React from 'react'

type NavigationMenuProps = {
  children: React.ReactNode
}

export const NavigationMenu = ({ children }: NavigationMenuProps) => {
  return (
    <div>
      {/* Placeholder; will be replaced with shadcn/ui Navigation Menu */}
      {children}
    </div>
  )
}
